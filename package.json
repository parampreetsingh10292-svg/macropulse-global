{
  "name": "macropulse-global",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --omit=dev",
    "start": "cd server && node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
